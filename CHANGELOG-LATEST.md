### Added
- Alchemy and brewing recipes now have an optional `place_as_block` field that handles whether the result should be placed as a block or put the cauldron's inventory.

### Fixed
- Milk now has a dedicated item so recipes that use it can be seen in JEI/RRV (thanks @ThePotatoArchivist)
- Fixed comparison logic in `CauldronContents` (thanks @ThePotatoArchivist)
- JEI recipes now display correctly.
- Alchemy recipes no longer ignore the potion field.